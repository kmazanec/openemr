import type { ReactElement } from 'react';

export function DashboardLanding(): ReactElement {
  return (
    <div>
      <h1>Patient Dashboard</h1>
      <p>No patient selected. Open a patient from the menu.</p>
    </div>
  );
}
